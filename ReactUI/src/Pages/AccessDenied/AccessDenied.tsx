const AccessDenied = () => {
  return (
    <div>You don't have Access to the page Back to login page 

        <button onClick={() => window.location.href='/UI/login'}>Back to login page</button>
    </div>

    
  )
}

export default AccessDenied